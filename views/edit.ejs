<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>编辑新闻 - 江西软件职业技术大学校园新闻系统</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>江西软件职业技术大学校园新闻系统</h1>
        <nav>
            <a href="/">首页</a>
            <a href="/add">添加新闻</a>
            <a href="/logout">注销 (<%= user.username %>)</a>
        </nav>
    </header>
    <main>
        <h2>编辑新闻</h2>
        <form action="/edit/<%= newsItem.id %>" method="POST" class="news-form">
            <div>
                <label for="title">标题：</label>
                <input type="text" id="title" name="title" value="<%= newsItem.title %>" required>
            </div>
            <div>
                <label for="category">分类：</label>
                <select id="category" name="category" required>
                    <% categories.forEach(cat => { %>
                        <option value="<%= cat %>" <%= newsItem.category === cat ? 'selected' : '' %>><%= cat %></option>
                    <% }); %>
                </select>
            </div>
            <div>
                <label for="content">内容：</label>
                <textarea id="content" name="content" required><%= newsItem.content %></textarea>
            </div>
            <button type="submit">更新新闻</button>
        </form>
    </main>
    <footer>
        <p>&copy; 2023 江西软件职业技术大学校园新闻系统</p>
    </footer>
</body>
</html>